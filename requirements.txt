# Core
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4

# Web / serving
Flask>=3.0
gunicorn>=21.2

# Utilities
requests>=2.31

# Optional deep-learning model (neural collaborative filtering).
# TensorFlow support varies by Python version/OS. The core system runs
# without it; install only if you want the NCF model:
#   pip install "tensorflow>=2.15"
